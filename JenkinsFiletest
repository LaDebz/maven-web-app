pipeline {
    agent any
    
    tools {
        // Define Maven tool with the name 'Maven3.9.4'
        maven 'Maven3.9.4'
    }
    
    stages {
        stage('Build') {
            steps {
                // Invoke Maven to clean the project
                sh 'mvn clean'
            }
        }
    }
}
